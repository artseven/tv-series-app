import React from 'react';
import LoaderSrc from '../../assets/loader.gif';

const Loader = props => (
    <div>
        <img
         alt="Loader icon"
         src={LoaderSrc} />
    </div>
);

export default Loader;